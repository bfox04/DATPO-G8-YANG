FLIP FAN
(PRINTED PART) Add za/zb zeroing process

add decalogue, gap, stagger values to gui.

	
Tune: 
	za/zb angling constant 5.whatever
	The physical gap/stagger between the airfoils at startup (hard minimum)
		Should only affect gap value? movement is still relative and can be zeroed


GAP AT ZERO IN THE Y: 75mm
GAP AT ZERO IN THE X: 229-230mm

add angle limits


-----------------------------------------------------------------------------------------------
CLAUDIAN:
Firmware (main.cpp):

(DONE NOT TESTED) Revert Y negation — remove the minus sign from stepsToMM_Y(), encoderMM_Y(), and the Y motion command targetDegrees calculation. Positive input = positive internal direction = upward.

(DONE NOT TESTED) Rename ZA/ZB to AoA Bottom/Top — change all serial output strings, command names stay ZA and ZB for brevity but responses and POSITIONS output say "AoA Bot" and "AoA Top". GUI labels change too.

Add GAP_OFFSET_MM constant — a const float at the top of the file next to the other constants, set to 0.0 as a TODO for you to measure and fill in. This is the physical distance between airfoils when Y=0.

Add computed values to POS: line — extend the POS: output with three new fields: GAP=, STAG=, DECA= calculated as Y + GAP_OFFSET_MM, X, and ZB - ZA respectively. The GUI parses these automatically since the parser handles any key=value pair.

GUI (WORKING_GUI.html):

Add three display cells to the position banner — Gap (mm), Stagger (mm), Decalage (deg), styled as a visually grouped trio, picking up from the POS: line fields.

Rename ZA/ZB labels everywhere — axis badges, jog labels, position banner cells all change from "ZA"/"ZB" to "AoA Bot"/"AoA Top" (or abbreviated to fit).